## 字符串函数

```py
str.len()  --- 统计每行的字符数

startswith(x) --- 判断一个字符串的首字母是否为x

users['name'] = users['name'].str[0].str.upper() + users['name'].str[1:].str.lower()
--- str[0],str[1:]指的是字符串的首字母和剩下字母;str.upper(),str.lower()指的是将字符变成大写和小写

str.match(xx) -- 匹配字符串，返回的是布尔值类型

str.contians(xx) -- 内容包括xx字符串，返回的是布尔值类型
```

### 正则表达式

```
1 ^：表示一个字符串或行的开头

2 [a-z]：表示一个字符范围，匹配从 a 到 z 的任何字符。

3 [0-9]：表示一个字符范围，匹配从 0 到 9 的任何字符。

4 [a-zA-Z]：这个变量匹配从 a 到 z 或 A 到 Z 的任何字符。请注意，你可以在方括号内指定的字符范围的数量没有限制，您可以添加想要匹配的其他字符或范围。

5 [^a-z]：这个变量匹配不在 a 到 z 范围内的任何字符。请注意，字符 ^ 用来否定字符范围，它在方括号内的含义与它的方括号外表示开始的含义不同。

6 [a-z]*：表示一个字符范围，匹配从 a 到 z 的任何字符 0 次或多次。

7 [a-z]+：表示一个字符范围，匹配从 a 到 z 的任何字符 1 次或多次。

8 .：匹配任意一个字符。

9. \.：表示句点字符。请注意，反斜杠用于转义句点字符，因为句点字符在正则表达式中具有特殊含义。还要注意，在许多语言中，你需要转义反斜杠本身，因此需要使用\\.

10 $：表示一个字符串或行的结尾。

```

### 去重

```python
df.drop_duplicates(subset='字段名')
```

### 统计行数/列数

```
统计行数
df.shape[0]
len(df)

统计列数
df.shape[1]

```

### 实现开窗函数

```
df['rank'] = df.groupby(by=['Department'])['Salary'].rank(ascending=False,method='dense')

groupby --- partition by
紧跟后面的字段是order by
method --- 窗口函数方法

```





